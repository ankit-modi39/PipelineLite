{
  "name": "mini-cicd",
  "version": "0.1.0",
  "description": "A minimal CI/CD server in Node.js — webhook-triggered builds with live log streaming.",
  "type": "module",
  "main": "server/app.js",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node server/app.js",
    "dev": "nodemon server/app.js"
  },
  "nodemonConfig": {
    "ignore": [
      "server/builds/**",
      "*.log"
    ]
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.21.2",
    "socket.io": "^4.8.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.7",
    "socket.io-client": "^4.8.1"
  }
}
