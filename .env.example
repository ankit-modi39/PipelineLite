PORT=4000
NODE_ENV=development

# GitHub signs each webhook with HMAC-SHA256 using this secret.
# Generate a strong one with:  openssl rand -hex 32
GITHUB_WEBHOOK_SECRET=replace_me_with_a_strong_random_string
